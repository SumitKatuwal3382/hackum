# Root environment file (can be committed ONLY if it contains no real secrets)
# For local development with Create React App, .env.local overrides this.
# DO NOT store production or high-privilege API keys directly in a client-side React app.
# Obtain a Groq API key: https://console.groq.com/keys

REACT_APP_GROQ_API_KEY=replace_with_dev_or_limited_key
# Optional model override (defaults handled in src/ai/groqClient.js)
# REACT_APP_GROQ_MODEL=llama3-70b-8192
